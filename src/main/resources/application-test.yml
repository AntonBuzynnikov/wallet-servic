spring:
  datasource:
    url: jdbc:h2:mem:wallet_db
    username: sa
    password:
    driver-class-name: org.h2.Driver
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master-test.yaml
  h2:
    console:
      enabled: true
      path: /h2-console